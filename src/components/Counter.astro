---
import { Image } from 'astro:assets';
import { AnimatedCounter } from './AnimatedCounter.tsx';
import ChromeLogo from '../images/chrome.svg';
---

<section class="py-20 px-4">
  <div class="max-w-6xl mx-auto text-center">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <AnimatedCounter type="incident" client:load />
      <AnimatedCounter type="global" client:load />
      <AnimatedCounter type="money" client:load />
    </div>

    <div class="mt-16 p-8 bg-gray-900/50 rounded-lg border border-gray-800">
      <p class="text-3xl font-bold text-white mb-2">
        "useEffect ran <span class="text-warning-red">73 times</span> in the last second"
      </p>
      <div class="flex items-center justify-center gap-2">
        <p class="text-gray-400">— Your</p>
        <Image
          src={ChromeLogo}
          alt="Chrome"
          width={20}
          height={20}
          class="inline-block opacity-70"
        />
        <p class="text-gray-400">browser console, probably</p>
      </div>
    </div>
  </div>
</section>
